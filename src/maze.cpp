#include "maze.h"

maze::maze(int w , int h){
    width = w;
    height = h;

    for (int i = 0 ; i < h ; i++){

      cell_vec_t coll;
      for (int x = 0 ; x < w ; x++){

      }
      cells.push_back(coll);
    }
  }
